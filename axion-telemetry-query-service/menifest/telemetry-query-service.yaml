apiVersion: v1
kind: Service

metadata:
  name: telemetry-query-service
  namespace: axion
  labels:
    app: telemetry-query

spec:
  type: ClusterIP

  selector:
    app: telemetry-query

  ports:
  - name: http
    protocol: TCP
    port: 8000
    targetPort: 8000
