apiVersion: v1
kind: Service

metadata:
  name: ingestion-service
  namespace: axion
  labels:
    app: ingestion

spec:
  type: ClusterIP

  selector:
    app: ingestion

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000